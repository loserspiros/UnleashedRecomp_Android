I've made the following changes to fix the release.yml workflow failure and improve build robustness:

1.  **UnleashedRecompLib/CMakeLists.txt**: Fixed a syntax warning/error by escaping double quotes in a FATAL_ERROR message.
2.  **Root CMakeLists.txt**: Moved find_package(TBB) from UnleashedRecomp/CMakeLists.txt to the root file to ensure it's available for all sub-projects, especially host tools.
3.  **tools/XenosRecomp/XenosRecomp/CMakeLists.txt**: Explicitly linked XenosRecomp against TBB::tbb on Linux/Android. This is necessary because XenosRecomp uses parallel algorithms (std::execution::par_unseq) which require TBB when compiling with GCC or Clang on Linux.
4.  **.github/workflows/release.yml**:
    *   Added support for ASSET_REPO and ASSET_REPO_TOKEN secrets to allow automated builds on push to main (logic ported from validate.yml).
    *   Updated the "Download Data" step to check out the private asset repository if the secrets are provided.
    *   Improved the "Download Data" logic to handle data from the private repo and better manage archive extractions and file placements.
    *   Updated Build APK step to use ANDROID_NDK_HOME consistent with setup steps.

I've verified the CMake syntax and run some basic unit tests (test_string_utils, test_version_utils) which pass.
